Usiamo la libreria Tkinter per la semplicità
-installare Python IDLE (Tkinter già integrato)

imortare la libreria tramite comando:
-import tkinter as tk

creare una nuova finestra con tk
-window=tk.Tk()

dare un titolo alla finestra
-window.title("<nome applicazione>")

dare le dimensioni alla finestra
-window.geometry("<larghezza>x<altezza>")

creare un label per la finestra
-<labelName> = tk.label(text = "<testo>")

dare al label la posizione tramite una griglia in alto a sinistra (0,0)
-<labelName>.grid(column=0, row=0)

chiamare il metodo mainloop(), il programma
-window.mainloop()

creare un bottone per la finestra
-<buttonName> = tk.Button(window, text = "<testo>" [, bg="<color>"][, font=("Times new roman", <size>)])
-<buttonName>.grid(column=1, row=0)

associare un evento ad un bottone
-<buttonName>.bind("<identificatore>", <richiamo evento(metodo)>)

creare un metodo che accetta un evento come argomento
-def <nomeFunzione>:
     <codice>print("Hello World!!!")

per accedere ad una pagina web bisogna importare webbrowser
-import webbrowser

Creazione di campi di immissione
-tk.Entry(<window o parent>,width=25)

???-tk.Text(<parent o master=window>,height=20,width=10)

Accedere al valore di un campo
-<>variabile = <nomeInput>.get()

Creazione di pulsanti di controllo
-tk.Checkbutton(<window o parent>,text="<testo>",variable=tk.IntVar())

meu opzioni come dropdown
-tk.OptionMenu(parent,tk.IntVar(),"Age","15+","25+","40+")

aggiungere una scrollbar
- tk.Scrollbar(parent, orient=tk.VERTICAL)

Possiamo collegare un pulsante con le voci utilizzando il "comand" parola chiave e assegnargli un nome di funzione.
- button = tk.Button(text="click",command=function_name)

answer = " Heyy {monkey}!!!. You are {age} years old!!! ".format(monkey=name, age=monkey.age())
- <tk.Text>.insert(tk.END,answer)

aprire un immagine e darli alcuni valori
-image=Image.open('app_image.jpeg')
-image.thumbnail((300,300),Image.ANTIALIAS)
-photo=ImageTk.PhotoImage(image)
-label_image=tk.Label(image=photo)
-label_image.grid(column=1,row=0)

creare un oggetto
-class <nomeOggetto>




Tkinter
PyQT
WxPython
PyGUI
PySimpleGUI
Kivy